// for of to loop array.
// for in to loop object.
const friends = ['wajed', 'ratan', 'goutom', 'pritom', 'anjan'];
//console.log(friends);
for(const friend of friends ){
    //console.log(friend);
}
for(let i = 0; i<friends.length; i++){
   // console.log(friends[i]);
}
let i=0;
while(i<friends.length){
    console.log(friends[i]);
    i++
}